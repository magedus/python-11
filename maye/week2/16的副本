#coding=utf-8
import random
lst=[]
#生成26个大写的字母
for x in range(65,91):
    a=str(chr(x))  #生成对应的ASCII码对应的字符串
    lst.append(a)
#生成26个小写字母
for x in range(97,123):
    a=str(chr(x))  #生成对应的ASCII码
    lst.append(a)
#生成10个数字
for x in range(10):
    lst.append(str(x))
i=0
def gen_code():
    s=''
    for _ in range(16):
        a=random.choice(lst)
        s+=a
    print(s)
for _ in range(200):
    gen_code()